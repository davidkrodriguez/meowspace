# Set to `postgres` to use the PostgreSQL adapter.
# Any other value (or unset) uses the in-memory adapter.
PERSISTENCE_DRIVER=postgres

# PostgreSQL connection string used when PERSISTENCE_DRIVER=postgres.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/meowspace

# Optional: disable SSL for local databases.
PGSSLMODE=disable
